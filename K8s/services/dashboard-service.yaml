apiVersion: v1
kind: Service
metadata:
  name: mqtt-broker
  namespace: digital-twin
spec:
  selector:
    app: mqtt-broker
  ports:
    - protocol: TCP
      port: 1883        # Service port
      targetPort: 1883  # Container port in mosquitto.conf
